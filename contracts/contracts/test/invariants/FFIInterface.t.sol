pragma solidity =0.8.16;

import {CommonTest} from "../CommonTest.t.sol";

contract FFIInterface is CommonTest {

    // function test_stake_withdraw() external {
    //     assertEq(alice, rollup.submitter());
    //     vm.deal(alice, 5 * MIN_DEPOSIT_);
    //     vm.startPrank(alice);
    //     rollup.stake{value: MIN_DEPOSIT_}();
    //     assertEq(MIN_DEPOSIT_, rollup.deposits(alice));

    //     rollup.withdraw(MIN_DEPOSIT_);
    //     assertEq(0, rollup.deposits(alice));
    // }
}